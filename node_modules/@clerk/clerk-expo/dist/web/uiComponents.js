"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var uiComponents_exports = {};
__export(uiComponents_exports, {
  CreateOrganization: () => CreateOrganization,
  GoogleOneTap: () => GoogleOneTap,
  OrganizationList: () => OrganizationList,
  OrganizationProfile: () => OrganizationProfile,
  OrganizationSwitcher: () => OrganizationSwitcher,
  SignIn: () => SignIn,
  SignInButton: () => SignInButton,
  SignInWithMetamaskButton: () => SignInWithMetamaskButton,
  SignOutButton: () => SignOutButton,
  SignUp: () => SignUp,
  SignUpButton: () => SignUpButton,
  UserButton: () => UserButton,
  UserProfile: () => UserProfile
});
module.exports = __toCommonJS(uiComponents_exports);
var import_clerk_react = require("@clerk/clerk-react");
var import_react_native = require("react-native");
var import_errors = require("../utils/errors");
const ErrorComponent = (componentName) => {
  import_errors.errorThrower.throw(`<${componentName}/> component is not supported in native environments.`);
};
function WrapComponent(component) {
  const commonentName = component.displayName.replace("withClerk(", "").replace(")", "");
  return import_react_native.Platform.select({
    // @ts-expect-error - This is just to make the function signature match the original
    native: () => () => ErrorComponent(commonentName),
    default: () => component
  })();
}
const SignIn = WrapComponent(import_clerk_react.SignIn);
const SignUp = WrapComponent(import_clerk_react.SignUp);
const UserButton = WrapComponent(import_clerk_react.UserButton);
const UserProfile = WrapComponent(import_clerk_react.UserProfile);
const OrganizationProfile = WrapComponent(import_clerk_react.OrganizationProfile);
const OrganizationSwitcher = WrapComponent(import_clerk_react.OrganizationSwitcher);
const OrganizationList = WrapComponent(import_clerk_react.OrganizationList);
const CreateOrganization = WrapComponent(import_clerk_react.CreateOrganization);
const SignInButton = WrapComponent(import_clerk_react.SignInButton);
const SignUpButton = WrapComponent(import_clerk_react.SignUpButton);
const SignOutButton = WrapComponent(import_clerk_react.SignOutButton);
const SignInWithMetamaskButton = WrapComponent(import_clerk_react.SignInWithMetamaskButton);
const GoogleOneTap = WrapComponent(import_clerk_react.GoogleOneTap);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CreateOrganization,
  GoogleOneTap,
  OrganizationList,
  OrganizationProfile,
  OrganizationSwitcher,
  SignIn,
  SignInButton,
  SignInWithMetamaskButton,
  SignOutButton,
  SignUp,
  SignUpButton,
  UserButton,
  UserProfile
});
//# sourceMappingURL=uiComponents.js.map